# Drop your background music .mp3 file here.

# Then enable it in the frontend by editing `frontend/src/App.tsx` or by
# extending the render request body to include a `music` field that the
# Remotion composition will pick up from the `music` prop.

# Example file name: soundtrack.mp3
